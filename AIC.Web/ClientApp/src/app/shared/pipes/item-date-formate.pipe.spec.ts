import { ItemDateFormatePipe } from './item-date-formate.pipe';

describe('ItemDateFormatePipe', () => {
  it('create an instance', () => {
    const pipe = new ItemDateFormatePipe();
    expect(pipe).toBeTruthy();
  });
});
